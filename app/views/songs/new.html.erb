<h1>Create a Song</h1>

<%= form_for @song  do |f| %>
  <%= f.label :title %>
  <%= f.text_field :title %>
  <%= f.label :artist_id %>
  <%= f.text_field :artist_id %>

  <%= f.collection_select :genre_name, Genre.all, :id, :name %>
  <br />

  <%= f.submit %>
<% end %>
